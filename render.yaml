# Render.com deployment configuration
# Deploy both frontend and backend from one repository

services:
  # Backend API (Node.js + Python) - Native Render web service (no Docker)
  - type: web
    name: ev-simulation-api
    runtime: node
    rootDir: server
    buildCommand: npm install && (python3 -m pip install --user -r ../requirements.txt || python -m pip install --user -r ../requirements.txt)
    startCommand: node server.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: "3000"
      - key: PYTHONPATH
        value: /opt/render/project/src
    healthCheckPath: /health
    plan: free

  # Frontend (React/Vite Static Site)
  - type: web
    name: ev-simulation
    runtime: static
    rootDir: client/Opt-Frontend
    buildCommand: npm install && npm run build
    staticPublishPath: dist
    headers:
      - path: /*
        name: Cache-Control
        value: public, max-age=0, must-revalidate
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
    envVars:
      - key: VITE_API_URL
        sync: false  # Set this manually to your backend URL after first deploy
    plan: free
